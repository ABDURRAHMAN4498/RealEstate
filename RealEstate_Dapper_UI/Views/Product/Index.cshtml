@model List<ResultProductDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Admin/Index.cshtml";
    int count = 0;
}

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-12">
            <div class="bg-light rounded h-100 p-4">
                <h6 class="mb-4">Kategory Listesi</h6>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">İlan Başlığı</th>
                                <th scope="col">Şehir</th>
                                <th scope="col">İlçe</th>
                                <th scope="col">Kategory</th>
                                <th scope="col">Fiyat</th>
                                <th scope="col">Tür</th>
                                <th scope="col">Sil</th>
                                <th scope="col">Güncelle</th>
                                <th scope="col">Sitede Gör</th>
                                <th scope="col">Günün Fırsatı</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <th scope="row">@count</th>
                                    <td>@item.title</td>
                                    <td>@item.city</td>
                                    <td>@item.district</td>
                                    <td>@item.categoryName</td>
                                    <td>@item.price.ToString("c")</td>
                                    <td>@item.type</td>
                                    <td><a href="/Product/DeleteProduct/@item.productId" class="btn btn-sm btn-outline-danger">Sil</a></td>
                                    <td><a href="/Product/UpdateProduct/@item.productId" class="btn btn-sm btn-outline-success">Güncelle</a></td>
                                    <td><a href="/Product/UpdateProduct/@item.productId" class="btn btn-sm btn-outline-info">Sitede Gör</a></td>
                                    <td>
                                        @if (item.DealOfTheDay==true)
                                        {
                                            <a href="/Product/ProductDealOfTheDayChangeToFalse/@item.productId" class="btn btn-sm btn-outline-danger">Pasif Yap</a>
                                            
                                        }
                                        else
                                        {
                                            <a href="/Product/ProductDealOfTheDayChangeToTrue/@item.productId" class="btn btn-sm btn-outline-secondary">Aktif Yap</a>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a href="/Product/CreateProduct/" class="btn btn-sm btn-outline-info">Yeni İlan Girişi</a>
                </div>
            </div>
        </div>
    </div>
</div>

